{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "required": ["replicaCount", "image", "service", "persistence"],
  "properties": {
    "replicaCount": {"type": "integer", "const": 1},
    "imagePullSecrets": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["name"],
        "properties": {
          "name": {"type": "string", "minLength": 1}
        }
      }
    },
    "image": {
      "type": "object",
      "required": ["repository", "pullPolicy"],
      "properties": {
        "repository": {"type": "string", "minLength": 1},
        "tag": {"type": "string"},
        "digest": {"type": "string", "pattern": "^$|^sha256:[0-9a-f]{64}$"},
        "pullPolicy": {"enum": ["Always", "IfNotPresent", "Never"]}
      },
      "anyOf": [
        {
          "properties": {
            "digest": {"type": "string", "pattern": "^sha256:[0-9a-f]{64}$"}
          },
          "required": ["digest"]
        },
        {
          "properties": {
            "tag": {"type": "string", "minLength": 1}
          },
          "required": ["tag"]
        }
      ]
    },
    "service": {
      "type": "object",
      "required": ["port", "targetPort"],
      "properties": {
        "port": {"type": "integer", "const": 8096},
        "targetPort": {"type": "integer", "const": 8096}
      }
    },
    "persistence": {
      "type": "object",
      "required": ["claimName", "accessMode", "size"],
      "properties": {
        "hostPath": {
          "description": "When enabled, chart emits a static hostPath PersistentVolume + PVC so Restricted PSA pods mount a PVC while the library remains on the host. Not a pod-level hostPath volume.",
          "type": "object",
          "properties": {
            "enabled": {"type": "boolean"},
            "path": {"type": "string", "minLength": 1}
          }
        },
        "claimName": {"type": "string", "const": "library"},
        "storageClassName": {"type": "string"},
        "accessMode": {"const": "ReadWriteOnce"},
        "size": {"type": "string", "minLength": 1}
      }
    },
    "namespace": {
      "type": "object",
      "properties": {
        "create": {"type": "boolean"},
        "name": {"type": "string", "const": "video"}
      }
    },
    "podSecurityContext": {
      "type": "object",
      "properties": {
        "runAsUser": {"type": "integer", "const": 10008},
        "runAsGroup": {"type": "integer", "const": 10008},
        "fsGroup": {"type": "integer", "const": 20000}
      }
    }
  }
}
