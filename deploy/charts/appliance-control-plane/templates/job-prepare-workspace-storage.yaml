{{- if and .Values.workspaceStorage.enabled .Values.workspaceStorage.prepareJob.enabled (eq (include "appliance-control-plane.argoWorkflowEnabled" .) "true") }}
{{- fail "workspaceStorage.prepareJob.enabled is not supported; prepare the workspace host directory with zonctl or manually before Helm runs" }}
{{- end }}
