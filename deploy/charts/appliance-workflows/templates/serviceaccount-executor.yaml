{{- if .Values.serviceAccount.executor.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "appliance-workflows.executorServiceAccountName" . }}
  namespace: {{ include "appliance-workflows.buildsNamespace" . }}
  labels:
    {{- include "appliance-workflows.labels" . | nindent 4 }}
automountServiceAccountToken: true
{{- end }}
