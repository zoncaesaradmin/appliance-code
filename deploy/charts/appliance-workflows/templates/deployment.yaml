apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "appliance-workflows.deploymentName" . }}
  namespace: {{ include "appliance-workflows.workflowsNamespace" . }}
  labels:
    {{- include "appliance-workflows.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.controller.replicas }}
  selector:
    matchLabels:
      {{- include "appliance-workflows.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/configmap.yaml") . | sha256sum }}
      labels:
        {{- include "appliance-workflows.labels" . | nindent 8 }}
        {{- include "appliance-workflows.selectorLabels" . | nindent 8 }}
    spec:
      serviceAccountName: {{ include "appliance-workflows.controllerServiceAccountName" . }}
      automountServiceAccountToken: true
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: workflow-controller
          image: {{ include "appliance-workflows.image" (dict "image" .Values.image.controller "Chart" .Chart) | quote }}
          imagePullPolicy: {{ .Values.image.controller.pullPolicy }}
          args:
            - --configmap
            - {{ include "appliance-workflows.configMapName" . }}
            - --executor-image
            - {{ include "appliance-workflows.image" (dict "image" .Values.image.executor "Chart" .Chart) | quote }}
            - --namespaced
            - --managed-namespace
            - {{ include "appliance-workflows.managedNamespace" . }}
            - --loglevel
            - {{ .Values.controller.logLevel }}
            {{- range .Values.controller.extraArgs }}
            - {{ . | quote }}
            {{- end }}
          env:
            - name: LEADER_ELECTION_IDENTITY
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
          securityContext:
            {{- toYaml .Values.containerSecurityContext | nindent 12 }}
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          volumeMounts:
            - name: appliance-logs
              mountPath: /data/zon/logs
      volumes:
        - name: appliance-logs
          hostPath:
            path: /data/zon/logs
            type: DirectoryOrCreate
