{{- if .Values.networkPolicy.enabled }}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "appliance-workflows.fullname" . }}-workflows-default-deny
  namespace: {{ include "appliance-workflows.workflowsNamespace" . }}
  labels:
    {{- include "appliance-workflows.labels" . | nindent 4 }}
spec:
  podSelector: {}
  policyTypes:
    - Ingress
    - Egress
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "appliance-workflows.fullname" . }}-builds-default-deny
  namespace: {{ include "appliance-workflows.buildsNamespace" . }}
  labels:
    {{- include "appliance-workflows.labels" . | nindent 4 }}
spec:
  podSelector: {}
  policyTypes:
    - Ingress
    - Egress
{{- end }}
