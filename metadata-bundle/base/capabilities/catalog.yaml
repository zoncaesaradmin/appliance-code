capabilities:
  base:
    displayName: Base
    requires: []
    conflicts: []
    license:
      requiredEntitlement: zon.capabilities.base
    artifacts:
      required: [control-plane-image, control-plane-chart]
    packages: [foundation]
  host:
    displayName: Host
    requires: [base]
    conflicts: []
    license:
      requiredEntitlement: zon.capabilities.host
    artifacts:
      required: [host-agent-image]
    packages: [deviceuser]
  files:
    displayName: Files
    requires: [base]
    conflicts: []
    license:
      requiredEntitlement: zon.capabilities.files
    artifacts:
      required: []
    packages: [foundation]
  workflows:
    displayName: Workflows
    requires: [base]
    conflicts: []
    license:
      requiredEntitlement: zon.capabilities.workflows
    artifacts:
      required: [workflow-templates]
    packages: [developer]
  build:
    displayName: Build
    requires: [base, host, workflows, artifact]
    conflicts: []
    license:
      requiredEntitlement: zon.capabilities.build
    artifacts:
      required: [workspace-provisioner-image, workflow-templates]
    packages: [developer]
  artifact:
    displayName: Artifact
    requires: [base]
    conflicts: []
    license:
      requiredEntitlement: zon.capabilities.artifact
    artifacts:
      required: [artifact-server-image, appliance-registry-chart]
    packages: [developer]
  dns:
    displayName: LAN DNS
    requires: [base, host]
    conflicts: []
    license:
      requiredEntitlement: zon.capabilities.dns
    artifacts:
      required: [coredns-image, appliance-dns-chart]
    packages: [developer]
  inference:
    displayName: Inference
    requires: [base]
    conflicts: []
    license:
      requiredEntitlement: zon.capabilities.inference
    artifacts:
      required: [inference-runtime-image, appliance-inference-chart]
    packages: [inference]
  video:
    displayName: Video
    requires: [base]
    conflicts: []
    license:
      requiredEntitlement: zon.capabilities.video
    artifacts:
      required: []
    packages: [foundation]
  guest-access:
    displayName: Guest Access
    requires: [base]
    conflicts: []
    license:
      requiredEntitlement: zon.capabilities.guest-access
    artifacts:
      required: []
    packages: [foundation]
  focus-content:
    displayName: Focus Content
    requires: [base, video]
    conflicts: []
    license:
      requiredEntitlement: zon.capabilities.focus-content
    artifacts:
      required: []
    packages: [foundation]
  applications:
    displayName: Applications
    requires: [base, host]
    conflicts: []
    license:
      requiredEntitlement: zon.capabilities.applications
    artifacts:
      required: [host-agent-image]
    packages: [deviceuser]
  plaintext-http:
    displayName: Plaintext HTTP
    requires: [base]
    conflicts: []
    license:
      requiredEntitlement: zon.capabilities.plaintext-http
    artifacts:
      required: []
    packages: [foundation]
